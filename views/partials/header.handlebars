<header>
  {{! Nav }}
  <div class="nav container">
    {{! Logo }}
    <a href="#" class="logo">Tune<span> IN </span>Live</a>
    {{! Login Button }}
    <a href="#" class="login">Login</a>
  </div>
</header>